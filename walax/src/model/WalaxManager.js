import w from '../Walax'
var m = require('mithril')
var { observable } = require('mobx')

class WalaxManager {
  _model = false

  constructor (model) {
    this._model = model
  }

  all() {
  }

  get(id) {
    
  }
}

export default WalaxManager
